// const addButtons = document.querySelectorAll('.gameTile > button')

// Array.from(addButtons).forEach((el)=>{
//     el.addEventListener('click', addGame)
// })

// async function addGame(){
//     const gameName = this.parentNode.dataset.gameName
//     const gameId = this.parentNode.dataset.gameId
// }